type Profile @model 
@auth(rules: [{ allow: owner, operations: [create, update, delete, read], identityClaim: "cognito:username" }]) {
  id: ID!
  name: String!
  location: String!
  price: String!
  description: String!
  imageUrl: String
  owner: String
}